# Logs & Monitoring using helm
------

To run the elk install, just run the bash script on the minikube cluster server with preinstalled helm.

helm-elk.sh will install Elasticsearch, Logstash and Kibana
